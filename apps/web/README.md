# Invest Copilote — Frontend (Next.js)

Voir le [README racine](../../README.md) pour le démarrage complet.

```bash
npm install
npm run dev   # http://localhost:3000 (API attendue sur :8000)
```

Astuce logo : dépose ton fichier original dans `public/` et remplace le composant
`src/components/Logo.tsx` si tu préfères le PNG à la version vectorielle.
